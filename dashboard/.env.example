# ============================================================
# Dashboard (Next.js) — Environment Variables
# ============================================================

# URL backend FastAPI (suite) — digunakan server-side oleh proxy route
# Set ini di Railway dashboard service Variables (BUKAN build-time, cukup runtime)
BACKEND_URL=https://your-backend-suite.up.railway.app

# (Legacy / opsional) — sudah tidak dipakai di login, tapi bisa dipakai komponen lain
# NEXT_PUBLIC_BACKEND_URL=https://your-backend-suite.up.railway.app
